import { escapeHTML } from '../utils/html.js';
export function mainMenuText(firstName = 'Developer') { return `⚡ <b>From Dev → DEV</b>\n\nHello <b>${escapeHTML(firstName)}</b>. Choose a developer workflow below, use slash commands for tools, or simply send a message to chat with the AI assistant.\n\n<b>Developer toolkit</b>\n• Preview, validate, and fix Telegram formatting\n• Ask Gemini programming and Telegram Bot API questions naturally\n• Use official Telegram documentation as the source of truth for Telegram guidance\n• Developer web app with live Rich Message preview`; }
export function modePrompt(mode) { return ({ preview:'📖 Send HTML, Markdown, or {"html":"..."}/{"markdown":"..."} to preview it as a Telegram Rich Message.', validate:'🛠 Send HTML, MarkdownV2/Rich Markdown, or an InputRichMessage JSON object to validate.', fix:'🔍 Send broken formatting. I will explain the issue and repair what can be repaired safely.', assistant:'🤖 Ask anything: JavaScript, debugging, Telegram Bot API, Rich Messages, MarkdownV2, code review, or best practices.' })[mode] || 'Choose a workflow.'; }
export function helpText({ hasGlobalGeminiKey, hasSessionGeminiKey, ownerConfigured }) { const geminiStatus = hasSessionGeminiKey ? 'configured for this chat session' : hasGlobalGeminiKey ? 'configured globally from GEMINI_API_KEY' : 'not configured yet'; return `❓ <b>Help</b>\n\n<b>Commands</b>\n/start, /menu, /help, /preview, /validate, /fix, /assistant, /convert, /examples, /docs, /settings, /about, /myid\n\n<b>AI Assistant</b>\nSend any non-command message while in assistant mode to chat naturally. For Telegram Bot API and Rich Messages questions, Gemini is instructed with official Telegram documentation context before it answers.\n\n<b>Gemini API key</b>\nCurrent status: <code>${geminiStatus}</code>. Owner-only temporary method: <code>/setgemini YOUR_API_KEY</code>. Owner protection: <code>${ownerConfigured ? 'configured' : 'not configured'}</code>.`; }
export function settingsText(session, config) { return `⚙️ <b>Settings</b>\n\nMode: <code>${session.mode}</code>\nDefault preview format: <code>${session.format}</code>\nRTL: <code>${session.isRtl ? 'on' : 'off'}</code>\nSkip entity detection: <code>${session.skipEntityDetection ? 'on' : 'off'}</code>\nGemini session key: <code>${session.geminiApiKey ? 'configured' : 'not configured'}</code>\nGlobal Gemini key: <code>${config.geminiApiKey ? 'configured' : 'not configured'}</code>\nOwner ID: <code>${config.botOwnerId || 'not configured'}</code>`; }
export const guideText = '<b>Formatting Guide</b>\n• Rich Messages send <code>{html}</code> or <code>{markdown}</code>, never structural <code>blocks</code>.\n• HTML supports rich Telegram formatting features such as headings, lists, tables, quotes, details, media blocks, formulas, custom emoji, and inline styles.\n• Use validation before sending complex formatting to Telegram.';
export const exampleText = '<b>Rich Messages</b>\n\nTry examples with <code>/preview</code> or in the web workbench.\n\n<pre>&lt;h1&gt;Release Notes&lt;/h1&gt;\n&lt;p&gt;&lt;b&gt;Bold&lt;/b&gt;, &lt;i&gt;italic&lt;/i&gt;, &lt;tg-spoiler&gt;spoiler&lt;/tg-spoiler&gt;&lt;/p&gt;</pre>';
export const aboutText = 'ℹ️ <b>About From Dev → DEV</b>\n\nA developer-focused Telegram assistant for previewing, validating, repairing, and understanding Telegram formatting with natural Gemini support.';
